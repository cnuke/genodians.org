server.port            = 80
server.document-root   = "/webroot"
server.event-handler   = "select"
server.network-backend = "write"
server.max-keep-alive-requests = 4
server.max-keep-alive-idle     = 4
server.max-connections = 4

index-file.names       = ("index")
mimetype.assign        = (
  ""      => "text/html"
)
